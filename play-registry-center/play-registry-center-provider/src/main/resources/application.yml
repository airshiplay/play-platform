server:
  port: 18888
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:18888/eureka
  server:
    enable-self-preservation: true
  environment: product
  data-center-info: default
spring:
  application:
    name: registry-center-server
  profiles:
    active: dev